# Set to false to fully exclude the AI demo pages/routes from the production build.
# Forks that don't want the AI demo section can also delete frontend/src/pages/ai-demos/.
VITE_ENABLE_AI_DEMOS=true
